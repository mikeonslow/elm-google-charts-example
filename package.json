{
  "name": "elm-google-charts-test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "npm run build && elm package install --yes && elm-live src/Main.elm --output=dist/js/app.js --dir=dist --pushstate --open --debug",
    "build": "mkdir -p dist && cp -r src/{*.html,js} dist",
    "watch": "npm-scripts-watcher"
  },
  "watch": {
    "src/{*.html,*.js}": [
      "build"
    ]
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/mikeonslow/elm-google-charts-test.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/mikeonslow/elm-google-charts-test/issues"
  },
  "homepage": "https://github.com/mikeonslow/elm-google-charts-test#readme",
  "dependencies": {
    "faker": "^4.1.0",
    "ramda": "^0.25.0"
  },
  "devDependencies": {
    "google-closure-compiler-js": "^20180204.0.0",
    "npm-scripts-watcher": "^1.0.2"
  }
}
